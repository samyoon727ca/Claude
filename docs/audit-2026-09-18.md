# Portfolio audit — 2026-09-18

A single autonomous consistency/hygiene pass over the whole repo, plus a
consolidated next-steps roadmap. Base: `main` (PR #6 merged) with PR #7
(`draytek-run3-tier3-reframe`) merged in, so this branch carries the full
run-3 n-day reframe *and* the framework-improvement-review doc.

Verification gate (`tools/run-checks.sh`, WSL Ubuntu): **28 passed, 0 failed**
before and after the changes below (28 = the clean-checkout 26 plus the two
git-ignored `work/` glyphicons SVGs; see [`../CLAUDE.md`](../CLAUDE.md) §Verification).

## What was audited

- **Run-3 novel→n-day consistency** — grepped the whole tree for `novel` /
  `no matching CVE` / `new CVE`. Every DrayTek-run-3-specific claim reads as an
  n-day (CVE-2024-45890) or as the corrected dedup-miss narrative; the remaining
  `novel` hits are generic method language in the runbook / target-selection /
  framework-review docs, which is intended.
- **Cert/clearance sync** — the trio flagged in CLAUDE.md agrees:
  [`qualification-map.md`](qualification-map.md) §8, the
  [`artifact-plan.md`](artifact-plan.md) clearance/cert section, and
  [`portfolio-blueprint.svg`](portfolio-blueprint.svg) cell 8 all read
  TS/SCI+SAP (held) · Security+ (held) · CASP+/SecurityX (held) · CISSP (in progress).
- **Gate count** — reconciled the documented "26 passed" against the observed 28.
- **Skills** — five skills, each with a coherent SKILL.md and scripts that parse
  and self-test; funnel smoke test green end-to-end.
- **Track 1/2/3 + P5.1 status** — claims match what is present (4 Track 2 docs
  180–243 lines; 5 skills 68–93 lines; mavlink-sectest self-test passes).
- **Research hygiene** — no firmware blobs committed; `.gitignore` covers all
  image/workspace paths; acquisition logs + `hashes.txt` present for all three
  runs; the reframed DrayTek `finding.json` still validates
  (`make_report --validate` → OK); no unfilled placeholders (CVE-XXXX / TODO /
  REPLACE) in committed paperwork.
- **Links** — internal-link check (gate §6, scans every `**/*.md`) green.

## What changed (all reversible, gate stayed green)

| Fix | Files | Why |
|-----|-------|-----|
| Skill count 6 → 5 | `README.md`, `NEXT-SESSION.md`, `portfolio-blueprint.svg` | There are five Skills; `setup-tools.sh` is a provisioning script inside firmware-triage, not a Skill. The SVG draws six chips (five skills + setup helper), so its caption now reads "5 skills + setup helper". |
| Gate count reconciled | `CLAUDE.md`, `NEXT-SESSION.md` | Clean checkout = 26; local `work/` extraction adds two SVGs → 28. Documented so 28 is not read as a regression. |
| Legend completed | `qualification-map.md` | Legend defined an unused `[cert]` but the body uses `[building]`, `[held]`, `[in progress]`; legend now matches. |

## Deliberately left alone

- **Run-3 novel↔n-day history.** The corrected dedup-miss narrative, the retired
  new-CVE paperwork, and the acquisition-log ledger are intentional truthful
  history — not tidied away.
- **Generic "novelty" language** in the runbook / target-selection / framework
  docs — it is about the general method, not the DrayTek finding.
- **Held disclosure artifacts.** The DrayTek coverage-gap notice
  ([`../research/draytek-vigor/vendor-report.md`](../research/draytek-vigor/vendor-report.md)
  + `disclosure-email.txt`) stays HELD. Nothing sent.
- **No file moves/renames.** No orphaned or stale files were found that warranted
  reorganization; the Ghidra `.java` scripts and `hashes.txt` are live evidence.

## Needs a human decision

- **Clearance on the public banner.** CLAUDE.md's doc-consistency note leaves open
  whether [`portfolio-blueprint.svg`](portfolio-blueprint.svg) cell 8 should surface
  the TS/SCI+SAP line (today it shows certs only). This is a deliberate
  publish-scope call, not an inconsistency — left to the owner.
- **Outbound disclosure.** Sending the DrayTek coverage-gap notice for
  CVE-2024-45890 (that it also affects the Vigor300B) is a user-confirmed step, and
  per [`disclosure-policy.md`](disclosure-policy.md) §5 the personal COI /
  outside-activity reporting is handled *first*.

## Consolidated next-steps roadmap

Ordered by the [`artifact-plan.md`](artifact-plan.md) "show > tell" reprioritization.
**Track A** is the serial deep-flow path; **Track B** items parallelize without
blocking A.

1. **[Track A · top] DrayTek model fan-out — the only remaining new-CVE path.**
   Grep the `download_ovpn` → `create_client_conf.sh` unquoted-args pattern (and the
   `FUN_0000ad8c` incomplete-blocklist sanitizer) across other Vigor models' public
   firmware, looking for a **still-supported, out-of-CPE, unpatched** model. Needs
   firmware downloads (user-confirmed). Ref:
   [`../research/draytek-vigor/acquisition-log.md`](../research/draytek-vigor/acquisition-log.md)
   §Fan-out.
2. **[Track A] Runtime PoC for the 300B n-day** on an owned/emulated ≤ 1.5.1.6
   device (QEMU/FirmAE) — converts the published case study from static-only to
   runtime-substantiated. Ref:
   [`../writeups/draytek-vigor300b-download_ovpn-cmdinjection.md`](../writeups/draytek-vigor300b-download_ovpn-cmdinjection.md).
3. **[Track A · human-gated] Send the DrayTek coverage-gap notice** (CVE-2024-45890
   also affects Vigor300B) — after COI/outside-activity disclosure. Held draft ready.
4. **[Track A] Execute the P5.1 UAS capstone.** Run `mavlink-sectest` against real
   ArduPilot/PX4 SITL and fold the results into the assessment; harness + threat
   model are built. Restart steps: [`NEXT-SESSION.md`](NEXT-SESSION.md) Path B.
5. **[Track B] FPGA security artifact (P6.1)** — the one named JD requirement with
   zero coverage: bitstream auth/encryption, eFUSE keying, PUFs, RoT into
   programmable logic. Extends the P4.2 secure-boot explainer.
6. **[Track B] One systems-language build (P6.2)** — Rust or C (e.g. a MAVLink v2
   signing impl or a C PoC for a confirmed finding), not Python glue.
7. **[Track B] One real brief (P6.3)** — a deck or recorded talk; the JD weights
   briefing government customers heavily and no *brief* artifact exists yet.

**Audit debt:** none outstanding — the three consistency fixes above are applied and
the gate is green. Revisit the clearance-on-banner decision when the portfolio goes
public.
